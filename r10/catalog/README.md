# R10 catalog — training layer (not combat Edge)

Per owner law: **prod dicts + systemic matcher + SFT seed through engine**.

| Piece | Path | Role |
|-------|------|------|
| **Dicts** | `data/brand_variants.json`, `data/model_aliases.json`, `data/r10_catalog_matcher_index.json` | Snapshot from prod (read-only) |
| **Matcher** | `matcher.py` | Python port of prod `r10CatalogMatcher.ts` — catalog-wide |
| **Regression** | `../scenarios/catalog-regression.json` | ≥2 brands per class; fiction `regression:catalog:` |
| **SFT seed** | `sft-seed/catalog-matcher-seed.jsonl` | Generated by `scripts/build-sft-catalog-seed.py` |

## Not here (combat only)

- `r10DeliveryOnly.ts`, cabinet ingest, routing — stay on `api.r10.kz`
- No `r10*` modules in `supabase/functions/`

## Commands

```bash
python3 scripts/catalog-eval.py
python3 scripts/build-sft-catalog-seed.py
```

## Refresh dicts from prod

One-time manual snapshot (no CI SSH). See `data/SNAPSHOT.md`.

Prod paths:

- `/opt/open-bsp/r10-knowledge/brand_variants.json`
- `/opt/open-bsp/r10-knowledge/model_aliases.json`
- `/opt/open-bsp/supabase/functions/_shared/r10_catalog_matcher_index.json`

After refresh: run `catalog-eval.py`, commit data + scenarios.
